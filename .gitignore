# Environment variables for docker-compose (see .env.example for the template)
.env
.env.local

# Local Claude Code files (settings.json is intentionally tracked; skill
# plugin dirs and machine-local settings are not, anywhere in the tree)
.claude/settings.local.json
**/.claude/skills/
**/.claude/scheduled_tasks.lock
 